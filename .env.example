# Contact form (Resend)
# Get an API key at https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxx

# Where to receive enquiries (comma-separated for multiple)
CONTACT_TO=hello@yourdomain.com

# Sender address. For testing use Resend's onboarding domain (sends only to CONTACT_TO).
# For production, use a verified domain in Resend (e.g. contact@yourdomain.com).
# Format: "Display Name <email@domain.com>"
CONTACT_FROM=UMS Contact <onboarding@resend.dev>
